# 빌드 산출물·캐시 (이미지 안에서 새로 빌드함)
build/
.gradle/
.kotlin/

# 비밀키 — 절대 이미지에 넣지 않는다 (FCM은 런타임 env로 주입)
secrets/

# 로컬 전용
docker-compose.yml
.git/
.idea/
.vscode/
.playwright-mcp/
*.log

# 문서 (런타임 불필요)
README.md
*.md
